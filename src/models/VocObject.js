class VocObj {
  constructor(name, truncated, rect, id) {
    this.name = name
    this.truncated = truncated
    this.rect = rect
    this.id = id
    this.name = `${id}`
  }
}

export default VocObj
